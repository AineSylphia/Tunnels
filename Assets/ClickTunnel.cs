using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ClickTunnel : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Sprite sprite;
    }

    // Update is called once per frame
    void Update()
    {

    }
}
